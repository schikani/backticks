let age: int;
let a: float;

< get_age(): int
{
    printl(`age = [age]`);
    return age;
}

@ set_age(value: int):
{
    age = value;
}

get_age();
set_age(25);
get_age();


@ publish(duration: int):
{
    printl(`~~publish~~`);
    sleep(duration);
}

< check_func()
{
    let ty = 90;
    if age == 25
    {
        let t = 5;
        printl(`~~1~~`);
        sleep(1);
        if a == 0
        {
            publish(1);
            printl(`~~20~~`);
            sleep(1);
            if a == 2
            {
                printl(`sdfsdf`);
            }
            if a != 0
            {
                printl(`a = [a]`);
                usleep(100000);
            }
            else
            {
                printl(`wd`);
            }

        }
    }
    let count = 0;
    loop until count < 10
    {
        if count == 4
        {
            printl(`FOUR`);
        }
        else
        {
            printl(`--------------------`);
        }
        printl(`count = [count]`);
        count = count + 1;
        usleep(100000);
    }
}

let count = 0;
loop
{
    if count >= 10
    {
        break;
    }
    count = count + 1;
    printl(`count = [count]`);
    usleep(100000);
}



check_func();