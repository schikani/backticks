let _str = `Hello `;
# printl(`_str = [_str]`);

_str += `World!`;
# printl(`_str = [_str]`);

@ func(): str
{
    let t = _str;
    return t;
}

@ _num(): int
{
    return 0;
}

@ check():
{
    let greet = `Hello World!`;
    if func() == `Hello World!`
    {
        printl(`MATCHED`);
    }
    else
    {
        printl(`NOT MATCHED`);
    }
}
check();
# let t = func();
# let y = _num();