import file1 as f1
import file2 as f2
import file3 as f3

let id = 4;
let file_name = `file4.bt`;

@ get_id(): int
{
    return id;
}

@ get_info(): str
{
    return file_name;
}

printl(`
    Hello from {f1.get_info()}! | ID = {f1.get_id()}
    Hello from {f2.get_info()}! | ID = {f2.get_id()}
    Hello from {f3.file_name}! | ID = {f3.id}
    Hello from {file_name}! | ID = {get_id()}
`);