# @ TODO
# let xyz[] = [True, False, True];


< func(): float[]
{
    # let xyz[] = [`Hello`, `World`, `!`];
    let xyz[] = [10.5, 23.6, 45.78];
    # let xyz[] = [10, 23, 45];


    return xyz;
}
let f = func();

let df = f[2];
printl(df);

for i, v in f
{
    printl(`{i}: {v}`);
}

# let a = f[0];
# let b = 45.6;
# let c = 80;
# let d = True;

# printl(`{a} | {b} | {c} | {d} | {f[0]}`);

# let ghi[] = [`5`, `5`, `5`, `5`];