# @ TODO
# let xyz[] = [True, False, True];


< func(): float[]
{
    let xyz[] = [0.2, 12.36, 56.22];
    return xyz;
}

let f = func();

for idx, v in f
{
    printl(`{idx}: {v}`);
}

# let a = input(`> `);

# # printl(strlen(a));

# if a == `hello`
# {
#     printl(`HELLO!!`);
# }

@ gta(): str
{
    let a = input(`> `);
    return a;
}

# gta();
printl(gta());
